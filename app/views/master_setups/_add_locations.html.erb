<div class="tab-pane fade" id="add_locations">
  <div class="col-lg-6 col-lg-offset-3"><br/>
    <h3>New Location</h3><br/>
<%= form_for(@master_setup) do |f| %>
  <% if @master_setup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@master_setup.errors.count, "error") %> prohibited this master_setup from being saved:</h2>

      <ul>
      <% @master_setup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-lg-12">
    <%= f.label :municipality %><br>
    <%= f.text_field :municipality,{:class => "form-control",:autocomplete=>"off", :required=> true} %>

  </div>
  <div class="field col-lg-12">
  <%= f.label :province, "Province:" %>
  <%= f.text_field :province, :class => "form-control",:autocomplete=>"off", :required=> true %>
  </div>

<div class="actions col-lg-3 pull-right">
  <%= f.submit "Save", :class =>'btn btn-mini' %>
</div>
<% end %>

</div>
</div>
