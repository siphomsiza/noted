<div class="tab-pane active fade in" id="add_locations">
  <div class="col-lg-6 col-lg-offset-3"><br/>
    <h3>New Location</h3>
<%= form_for(@master_setup) do |f| %>
  <% if @master_setup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@master_setup.errors.count, "error") %> prohibited this master_setup from being saved:</h2>

      <ul>
      <% @master_setup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-lg-12">
    <%= f.label :municipality %><br>
    <%= f.text_field :municipality,{:class => "form-control"} %>

  </div>
  <div class="field col-lg-12">
  <%= f.label :province, "Province:" %>
  <%= f.text_field :province, :class => "form-control",:autocomplete=>"off" %>
  </div>
  <div class="col-lg-12">
  <div class="row"><div class="col-lg-12">
  <%= f.fields_for :regions do |regions_for_form| %>

    <%= render 'regions_fields', f: regions_for_form %>

<% end %>
<%= link_to_add_fields "Add Region/Area", f, :regions %>
</div></div>
</div>
  <div class="actions col-lg-3 col-lg-offset-4">
    <%= f.submit "Save", :class=> "btn btn-primary  pull-left" %>
  </div>
<% end %>
</div>
</div>
