<% provide(:title, "All SDBIPs") %>
<div id="setup">
  <h2>
    Step 1: System Setup
  </h2>
  <div class="row">
    <div class="col col-lg-12">
      <% if @setup.blank? %>
        <%= link_to "Configure Setup", new_setup_path, :class => "btn btn-mini" %>
      <% else %>
        <p>There is currently no setup configured.</p>
      <% end %>
      <%= link_to departments_path, :class =>"btn btn-midi pull-right" do %>
        Step 2: Department Setup <span class="glyphicon glyphicon-circle-arrow-right"></span>
      <% end %>
    </div>
    <div class="col col-lg-12">
      <table class="table table-hover">
        <thead>
          <tr>
            <th>&nbsp;</th>
            <th>Setup</th>
            <th>Date Created</th>
            <th>Date Updated</th>
          </tr>
        </thead>
        <tbody>
          <% @setup.each do |setup| %>
            <tr class="<%= cycle('odd', 'even') %>">
              <td>
                <input id="performance_comments" name="performance_comments" type="checkbox"
                  <% if setup.performance_comments? %>
                  checked= true
          <% end %>
            /></td>
           <td>Are the performance comment/corrective measures fields for the KPI Results required?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td><%= setup.performance_length %></td>
           <td>If the performance comment/corrective measures fields are required,&nbsp;what is the of minimum characters required?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td><input id="project_life_cycle" name="project_life_cycle" type="checkbox"
           <% if setup.project_life_cycle? %>
           checked= true
           <% end %>
            /></td>
           <td>Use project life cycle?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td><input id="attach_poe" name="attach_poe" type="checkbox"
           <% if setup.attach_poe? %>
           checked= true
           <% end %>
            />
           </td>
           <td>Allow users to attach POE when updating Departmental/Top Layer KPIs?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td>
            <input id="include_departmental_previous_comments" name="include_departmental_previous_comments" type="checkbox"
           <% if setup.include_departmental_previous_comments? %>
           checked= true
           <% end %>
            />
           </td>
           <td><b>Departmental SDBIP:</b>&nbsp;Include the target/actual for the previous time period?</td>
           <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td>
              <input id="include_departmental_next_target" name="include_departmental_next_target" type="checkbox"
           <% if setup.include_departmental_next_target? %>
           checked= true
           <% end %>
            />

           </td>
           <td><b>Departmental SDBIP:</b>&nbsp;Include the comments for the previous time period?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td>
              <input id="include_toplayer_previous_target" name="include_toplayer_previous_target" type="checkbox"
           <% if setup.include_toplayer_previous_target? %>
           checked= true
           <% end %>
            />
           </td>
           <td><b>Departmental SDBIP:</b>&nbsp;Include the target for the next time period?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
           <tr class="<%= cycle('odd', 'even') %>">
           <td>
             <input id="include_toplayer_next_target" name="include_toplayer_next_target" type="checkbox"
           <% if setup.include_toplayer_next_target? %>
           checked= true
           <% end %>
            />
           </td>
           <td><b>Top Layer SDBIP:</b>&nbsp;Include the comments for the previous time period?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
          <tr class="<%= cycle('odd', 'even') %>">
           <td>
            <input id="include_toplayer_previous_comments" name="include_toplayer_previous_comments" type="checkbox"
           <% if setup.include_toplayer_previous_comments? %>
           checked= true
           <% end %>
            />

           </td>
           <td><b>Top Layer SDBIP:</b>&nbsp;Include the target for the next time period?</td>
            <td><%= setup.created_at %></td>
             <td><%= setup.updated_at %></td>
          </tr>
        <tr><td colspan="4"><%= link_to edit_setup_path(setup), class: "btn btn-mini", :title=>"Click to edit KPI" do %>
              <span class="glyphicon glyphicon-pencil"></span>&nbsp;Edit
            <% end %></td></tr>

        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
