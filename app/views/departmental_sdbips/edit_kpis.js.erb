$(function(){
  $(document).on('change','#selected_department_edit', function(){
     var select_value = $(this).val();
     $('#current_department_edit').val(select_value);
     alert( "Handler for .change() called." + select_value );
  });
  $(document).on('change','#selected_subdepartment_edit', function(){
     var select_value = $(this).val();
     alert( "Handler for .change() called." + select_value );
     $('#current_subdepartment_edit').val(select_value);

  });
});

$("#departmental-sdbip-edit-modal").html("<%= j(render 'edit_kpis') %>")
$("#departmental-sdbip-edit-modal").modal("show")
$(document).ready(function(){
    $('input[type="submit"]').click(function(){
        $("#departmental-sdbip-edit-modal").modal("hide")
    });
});
