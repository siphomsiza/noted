<% if @capital_project.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@capital_project.errors.count, "error") %>
        prohibited this capital_project from being saved:</h2>

    <ul>
        <% @capital_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>

<% if @monthly_cashflow.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@monthly_cashflow.errors.count, "error") %>
        prohibited this monthly_cashflow from being saved:</h2>

    <ul>
        <% @monthly_cashflow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>

<% if @top_layer_sdbip.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@top_layer_sdbip.errors.count, "error") %>
        prohibited this top_layer_sdbip from being saved:</h2>

    <ul>
        <% @top_layer_sdbip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>

<% if @revenue_by_source.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@revenue_by_source.errors.count, "error") %>
        prohibited this revenue_by_source from being saved:</h2>

    <ul>
        <% @revenue_by_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>
